<!DOCTYPE html>
<html>
  <head>
    <title>The Iron Yelp</title>
  </head>
  <body>
    <h1>The Iron Yelp</h1>
    <a href="/restaurants"><h2>List of All Restaurants</h2></a>
    <h2>Restaurants By Cuisine Type</h2>
    <ul style="list-style: none;">
      <% @categories.each do |category| %>
      <a href= <%= "#{category.gsub(" ","_").downcase}" %>><li><%= category %></li></a>
      <% end %>
    </ul>
    <h2>Restaurants by Rating and Price</h2>
    <ul style="list-style: none;">
      <% @filters.each do |category| %>
        <li><a href= <%= "/#{category}" %> > <%= category.gsub("_", " ").capitalize %> </a></li>
      <% end %>
    </ul>
  </body>
</html>