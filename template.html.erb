<!DOCTYPE html>
<html>
<head>
  <title>Car Properies</title>
</head>
<body>
  <h1>Embedded Ruby and Cars!</h1>
  <p>I have <%= @cars.length %> cars</p>
  <table>
    <tr>
      <th>Manufacturer</th>
      <th>Model</th>
      <th>Size</th>
      <th>Base Price</th>
    </tr>
    <% @cars.sort_by{|car| car.manufacturer}.each do |car| %>
      <tr>
        <td><%= car.manufacturer %></td>
        <td><%= car.name %></td>
        <td><%= car.size %></td>
        <td><%= car.base_price %></td>
      </tr>
    <% end %>
  </table>
  <p><a href="home">Home</a></p>
  <p><a href="shutdown">Shutdown</a></p>
</body>
</html>